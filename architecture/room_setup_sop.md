# Room Setup SOP
1. Define Entities (`RoutineEntity`, `TaskEntity`).
2. Define DAOs (`RoutineDao` with relations).
3. Create AppDatabase.
4. Set up dependency injection (or manual instantiation) for Database.
